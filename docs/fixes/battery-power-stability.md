# Исправление проблемы перезагрузки при питании от батареи

## Проблема
Устройство перезагружается при вводе PIN кода когда питается от батареи через JST разъем, но работает стабильно от USB.

### Критичное наблюдение 🔍
**Ключевое открытие:** Устройство перезагружается только при **удержании кнопки**, но работает стабильно при **быстрых одиночных нажатиях**.

**Анализ:**
- Быстрое нажатие → 1 обновление экрана → короткий скачок тока → OK ✅
- Удержание кнопки → множественные обновления (каждые 250ms) → непрерывная нагрузка → brownout reset ❌

## Причина
Комбинация факторов вызывает просадку напряжения ниже порога brownout detector (2.43V по умолчанию):

1. **Множественные обновления экрана при удержании кнопки** - главная причина!
   - При удержании кнопки экран обновляется каждые 250ms (debounce интервал)
   - Каждое обновление: fillRect + drawString = скачок тока ~50-100mA
   - Непрерывная последовательность скачков не дает напряжению восстановиться

2. **Высокое потребление дисплея** - TFT дисплей с подсветкой на 100% яркости потребляет ~80-120mA

3. **Скачки тока при нажатии кнопок** - особенно GPIO 0 (BUTTON_2) может вызывать скачки

4. **Внутреннее сопротивление батареи** - старые или слабые батареи имеют высокое внутреннее сопротивление

5. **Отсутствие сглаживающих конденсаторов** - на линии питания нет достаточной емкости для сглаживания скачков

## Программные исправления (реализованы)

### 1. Ожидание отпускания кнопки перед обновлением экрана ⭐ КРИТИЧНОЕ
```cpp
// В pin_manager.cpp - requestPin()
if (digitalRead(BUTTON_X) == LOW && (millis() - lastButtonPress > debounce)) {
    lastButtonPress = millis();
    // ... обработка нажатия ...
    
    // 🔋 КРИТИЧНО: Ждем отпускания кнопки
    while(digitalRead(BUTTON_X) == LOW) {
        esp_task_wdt_reset();
        delay(10);
    }
    
    // 🔋 Задержка после отпускания для стабилизации
    delay(50);
    esp_task_wdt_reset();
    
    // Только ТЕПЕРЬ обновляем экран (1 раз, а не множество)
    updatePinScreen(...);
}
```

**Эффект:** Предотвращает множественные обновления экрана при удержании кнопки. Теперь каждое нажатие = только 1 обновление экрана, независимо от длительности удержания.

### 2. Снижение яркости дисплея при вводе PIN
```cpp
// В pin_manager.cpp - requestPin()
displayManager.setBrightness(128); // 50% яркости вместо 100%
```
Это снижает потребление дисплея с ~100mA до ~50mA.

### 3. Увеличение debounce времени
```cpp
const int debounce = 350; // Было 250ms, теперь 350ms
```
Уменьшает максимальную частоту возможных обновлений экрана.

### 4. Задержки для стабилизации питания
```cpp
// После отпускания кнопки, перед обновлением экрана
delay(50);
esp_task_wdt_reset();
```
Дает время конденсаторам восстановиться после скачка тока.

### 5. Настройка brownout detector
В `platformio.ini` и `sdkconfig.defaults`:
```ini
-DCONFIG_ESP32_BROWNOUT_DET_LVL=0  # 2.43V (default)
```

Можно попробовать понизить до 2.33V (уровень 1) если проблема сохраняется:
```ini
-DCONFIG_ESP32_BROWNOUT_DET_LVL=1  # 2.33V
```

⚠️ **ВНИМАНИЕ**: Слишком низкий порог brownout может привести к нестабильной работе и повреждению данных!

## Аппаратные рекомендации

### 1. Добавить сглаживающий конденсатор (РЕКОМЕНДУЕТСЯ)
Добавьте электролитический конденсатор **470µF - 1000µF, 6.3V** параллельно JST разъему:
- Положительная нога к VCC (красный провод JST)
- Отрицательная нога к GND (черный провод JST)

Это сгладит скачки напряжения при нажатии кнопок.

### 2. Добавить керамический конденсатор
Дополнительно добавьте керамический конденсатор **100nF (0.1µF)** рядом с ESP32:
- Максимально близко к пинам VCC и GND ESP32
- Для фильтрации высокочастотных помех

### 3. Использовать качественные батареи
- Используйте свежие Li-Ion/Li-Po батареи с низким внутренним сопротивлением
- Избегайте старых или дешевых батарей
- Рекомендуемая емкость: минимум 500mAh для стабильной работы

### 4. Проверить JST разъем
- Убедитесь что контакты JST разъема чистые и плотно прилегают
- Окисленные контакты увеличивают сопротивление

## Диагностика

### Проверка напряжения батареи
Добавьте в код мониторинг напряжения:
```cpp
// В loop() или при вводе PIN
int voltage = analogRead(34); // ADC pin для батареи
float batteryVoltage = (voltage / 4095.0) * 2.0 * 3.3; // Если есть делитель 1:2
LOG_INFO("Battery", "Voltage: " + String(batteryVoltage) + "V");
```

Если напряжение падает ниже 3.0V при нажатии кнопок - батарея слабая или нужен конденсатор.

### Проверка brownout событий
Добавьте в `setup()`:
```cpp
esp_reset_reason_t reset_reason = esp_reset_reason();
if (reset_reason == ESP_RST_BROWNOUT) {
    LOG_ERROR("Main", "Last reset was due to BROWNOUT!");
}
```

## Тестирование

1. **Без изменений**: Устройство перезагружается при вводе PIN от батареи ❌
2. **С программными исправлениями**: Снижение яркости + задержки ✅
3. **С конденсатором 470µF**: Максимальная стабильность ✅✅

## Дополнительные меры (если проблема сохраняется)

### 1. Динамическое управление частотой CPU
```cpp
// Перед вводом PIN
setCpuFrequencyMhz(160); // Снизить с 240MHz до 160MHz

// После ввода PIN
setCpuFrequencyMhz(240); // Вернуть обратно
```

### 2. Отключение WiFi/BLE при вводе PIN
```cpp
// Перед вводом PIN
WiFi.mode(WIFI_OFF);
btStop();

// После ввода PIN
WiFi.mode(WIFI_STA);
```

### 3. Увеличение debounce времени
```cpp
const int debounce = 300; // Было 250ms, увеличить до 300ms
```

## Итоговые рекомендации

**Минимальное решение** (программное):
- ✅ Снижение яркости до 50% при вводе PIN
- ✅ Задержки 10ms перед обновлением экрана
- ✅ Восстановление яркости после ввода

**Оптимальное решение** (программное + аппаратное):
- ✅ Все программные исправления
- ✅ Конденсатор 470µF на JST разъеме
- ✅ Керамический конденсатор 100nF у ESP32
- ✅ Качественная батарея ≥500mAh

**Если проблема сохраняется**:
- Попробуйте понизить brownout level до 2.33V (уровень 1)
- Добавьте динамическое управление CPU частотой
- Отключайте WiFi/BLE при вводе PIN

## Связанные файлы
- `src/pin_manager.cpp` - основные исправления
- `platformio.ini` - настройки brownout
- `sdkconfig.defaults` - конфигурация питания
